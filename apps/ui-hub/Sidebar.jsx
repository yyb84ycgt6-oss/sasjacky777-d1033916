import React from 'react';

// Placeholder for the sidebar component containing all tool widgets:
// - Router health check (GET /ready, GET /health)
// - Agent registry viewer (list agents, pods, backpacks)
// - Pod/Backpack manager info (preferred models per pod, hardware hints)
// - State viewer API endpoints (/agents, /pods, /backpacks, /agent-states)

export default function Sidebar() {
  return <div>Sidebar placeholder</div>;
}
